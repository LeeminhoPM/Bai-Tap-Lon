@model ECommerceWeb.Data.Models.Product
@using ECommerceWeb.Data.Models

@{
    Layout = "_Layout";

    // Lấy danh sách ảnh
    var images = Model.ProductImages?
                    .OrderByDescending(x => x.DefaultImage)
                    .ToList() ?? new List<ProductImage>();

    // Nếu không có ảnh -> dùng ảnh mặc định
    if (!images.Any())
    {
        images.Add(new ProductImage
        {
            ImageUrl = "/images/product-default.jpg",
            DefaultImage = true
        });
    }
}

<section class="sec-product-detail bg0 p-t-65 p-b-60">
    <div class="container">
        <div class="row">

            <!-- HÌNH ẢNH -->
            <div class="col-md-6 col-lg-7 p-b-30">
                <div class="p-l-25 p-r-30 p-lr-0-lg">
                    <div class="wrap-slick3 flex-sb flex-w">
                        <div class="wrap-slick3-dots"></div>
                        <div class="wrap-slick3-arrows flex-sb-m flex-w"></div>

                        <div class="slick3 gallery-lb">
                            @foreach (var img in images)
                            {
                                string url = img.ImageUrl.StartsWith("/") ? img.ImageUrl : "/images/products/" + img.ImageUrl;

                                <div class="item-slick3" data-thumb="@url">
                                    <div class="wrap-pic-w pos-relative">
                                        <img src="@url" alt="@Model.ProductName" />

                                        <a class="flex-c-m size-108 how-pos1 bor0 fs-16 cl10 bg0 hov-btn3 trans-04"
                                           href="@url">
                                            <i class="fa fa-expand"></i>
                                        </a>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>

            <!-- THÔNG TIN -->
            <div class="col-md-6 col-lg-5 p-b-30">
                <div class="p-r-50 p-t-5 p-lr-0-lg">

                    <h4 class="mtext-105 cl2 js-name-detail p-b-14">
                        @Model.ProductName
                    </h4>

                    <span class="mtext-106 cl2">
                        @Model.Price.ToString("N0") VNĐ
                    </span>

                    <p class="stext-102 cl3 p-t-23">
                        @Model.ShortDescription
                    </p>

                    <div class="p-t-33">
                        <div class="flex-w flex-r-m p-b-10">
                            <div class="size-204 flex-w flex-m respon6-next">
                                <div class="wrap-num-product flex-w m-r-20 m-tb-10">
                                    <div class="btn-num-product-down cl8 hov-btn3 trans-04 flex-c-m">
                                        <i class="fs-16 zmdi zmdi-minus"></i>
                                    </div>

                                    <input class="mtext-104 cl3 txt-center num-product" type="number"
                                           name="num-product" value="1">

                                    <div class="btn-num-product-up cl8 hov-btn3 trans-04 flex-c-m">
                                        <i class="fs-16 zmdi zmdi-plus"></i>
                                    </div>
                                </div>

                                <button class="flex-c-m stext-101 cl0 size-101 bg1 bor1 hov-btn1 p-lr-15 trans-04">
                                    Thêm vào giỏ hàng
                                </button>
                            </div>
                        </div>
                    </div>

                    <div class="p-t-65">
                        <span class="stext-102 cl3">
                            SKU: @Model.ProductId
                        </span>

                        <p class="stext-102 cl3">
                            Danh mục: @(Model.Category?.CategoryName ?? "Không xác định")
                        </p>
                    </div>

                </div>
            </div>
        </div>

        <!-- MÔ TẢ CHI TIẾT -->
        <div class="bor10 m-t-50 p-t-43 p-b-40">
            <div class="tab01">
                <ul class="nav nav-tabs" role="tablist">
                    <li class="nav-item p-b-10">
                        <a class="nav-link active" data-toggle="tab" href="#description" role="tab">Mô tả</a>
                    </li>
                </ul>

                <div class="tab-content p-t-43">
                    <div class="tab-pane fade show active" id="description" role="tabpanel">
                        <div class="how-pos2 p-lr-15-md">
                            <p class="stext-102 cl6 p-b-26">
                                @Html.Raw(Model.LongDescription)
                            </p>
                        </div>
                    </div>
                </div>

            </div>
        </div>

    </div>
</section>
